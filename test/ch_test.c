#include<stdio.h>
int main()
{
  // char *ch = " ";
  // if(ch == 0) {
  char ch;
  if(ch == printf("")) {
    printf("matched\n");
  } else {
    printf("doesn't matched");
  }
  printf("");
  return 0;
}
